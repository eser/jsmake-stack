//? if (target === 'playground') {
import * as immunity from '../immunity/index';

function test() {
    console.log('here is:');
    console.log(immunity.test);
    console.log(immunity.test());
}

export {
    test,
};
//? }
